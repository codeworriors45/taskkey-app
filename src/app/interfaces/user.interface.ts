export interface User {
    userId: string;
    email: string;
    name: string;
    lastName: string;
    phone?: string;
    publicKey?: string;
    profileImage?: string;
    uuid?: string;
    timeStamp?: any;
}
